# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Scrap Mechanic Search Engine — a search tool for the game Scrap Mechanic. The codebase is in early setup; no source files exist yet beyond `.gitattributes`.

## Getting Started

As the project grows, document build/dev/test commands here. Typical entries will look like:

```
npm install       # install deps
npm run dev       # start dev server
npm run build     # production build
npm test          # run tests
```

Update this file once `package.json` and the project structure are established.
